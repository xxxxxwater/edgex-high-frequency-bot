# Screenshots to add

Add the following images to `assets/screenshots/` and reference them in README:

1. `runtime-logs.png` - bot startup and live monitoring logs
2. `docker-deploy.png` - docker compose running status
3. `trade-report.png` - sample monitoring/performance report
4. `config-example.png` - sanitized config layout

Do not include API keys, account IDs, balances, or private endpoints.
